---
- name: Run nvidia-smi and show output
  hosts: all
  gather_facts: no
  tasks:
    - name: Execute nvidia-smi
      command: ls
      register: result
      changed_when: false

    - name: Display output
      debug:
        msg: "{{ result.stdout_lines }}"
